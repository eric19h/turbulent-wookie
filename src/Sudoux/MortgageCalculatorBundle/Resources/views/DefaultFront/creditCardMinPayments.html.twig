{% extends site.getThemeTemplate %}

{% block title %}Paying off Credit Card Debt with Minimum Payments{% endblock %}

{% block body_class %}page-mortgage-calculator{% endblock %}

{% block breadcrumb %}
	<ul class="breadcrumb">
		<li><a href="{{ path('sudoux_mortgage_calculator') }}">Mortgage Calculators</a> <span class="divider">/</span></li>	
		<li class="active">{{ block('title') }}</li>
	</ul>
{% endblock %}

{% block content %}
	<iframe id="iframe_calculator" width="100%" height="1200" style="border: 0px" scrolling="yes" frameborder="0" src="{{ calculator_url }}/website/includes/plugins/calculators/credit-card-minimum-payment.php"></iframe>
    {#<form action="{{ path('sudoux_mortgage_calculator_credit_card_min_payments') }}" method="post" {{ form_enctype(form) }} novalidate="novalidate">
        <div class="row">
            <div class="span4 offset4">
                <div class="form-horizontal">
                    <h3>Credit Card Balance and Annual Interest Rate</h3>
                    <div class="control-group">
                        {{ form_label(form.current_credit_card_balance, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
                        <div class="controls">
                            {{ form_widget(form.current_credit_card_balance) }}
                        </div>
                    </div>

                    <div class="control-group">
                        {{ form_label(form.interest_rate, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
                        <div class="controls">
                            {{ form_widget(form.interest_rate) }} %
                        </div>
                    </div>
                    <h3>Minimum Payment Requirement from the Most Recent Statement</h3>
                    <div class="control-group">
                        {{ form_label(form.current_min_payment, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
                        <div class="controls">
                            {{ form_widget(form.current_min_payment) }}
                        </div>
                    </div>
                    <h3>Fixed Payment You Can Afford To Pay Every Month</h3>
                    <div class="control-group">
                        {{ form_label(form.fixed_payment, label|default(null), { 'label_attr': { 'class': 'control-label' } }) }}
                        <div class="controls">
                            {{ form_widget(form.fixed_payment) }} (Months)
                        </div>
                    </div>
                </div>
            </div>

        </div>

        <div class="row">
            <div class="span12">
                <div class="submit-wrapper text-center">
                    <div class="hide">{{ form_rest(form) }}</div>
                    <input type="submit" value="Calculate" class="btn">
                </div>
            </div>
        </div>
    </form>#}
{% endblock %}